class Sale {
  constructor(id, product, quantity, price) {
    this.id = id;
    this.product = product;
    this.quantity = quantity;
    this.price = price;
  }

  // Outros métodos da classe Sale, se necessário
}

module.exports = Sale;
